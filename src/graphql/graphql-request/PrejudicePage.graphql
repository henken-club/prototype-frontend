query PrejudicePage($posted: ID!, $recieved: ID!, $number: Int!) {
  getPrejudice(
    input: {
      posted: {alias: $posted}
      received: {alias: $recieved}
      number: $number
    }
  ) {
    prejudice {
      title
      number
      userTo {
        alias
        displayName
        picture
      }
      userFrom {
        alias
        displayName
        picture
      }
    }
  }
}
